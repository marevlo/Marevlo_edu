# Reels worker extras — build a worker image with:
#   pip install -r requirements.txt -r requirements-worker.txt
# Only needed to enable Whisper auto-captions; HLS transcoding (MediaConvert)
# works with the base API image. Kept separate from requirements.txt so the
# API image stays lean (faster-whisper -> ctranslate2 pulls large NVIDIA CUDA
# wheels on Linux).
-r requirements.txt

# CPU-only Whisper. ctranslate2 ships a CPU build; we don't install the CUDA
# extras, so transcription runs on CPU (fine for the <=5-min reel clips).
faster-whisper==1.0.3
